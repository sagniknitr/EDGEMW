language: c
compiler: gcc clang
before_install:
  - pip install --user cpp-coveralls

script:
  - bash build.sh clang
  - bash build.sh

after_success:
  - pip install --user cpp-coveralls
  - coveralls --exclude testing --gcov-options '\-lp'
