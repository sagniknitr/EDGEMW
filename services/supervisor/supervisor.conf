# process 1
process_name = "SupervisorTest";

process_fullpath =  "."

process_args = "-g -b -c -d -f -e";

process_restart_count = 3; # 0 means unlimited

## if processes with higher bases are stopped, they will only be restarted because there's no dependency
process_dependency_level = 1; # 1 - base 2 - next higher of base

# process 2
process_name = "zw"

process_fullpath = "/usr/bin/";

process_args = "-m -l";

process_restart_count = 3;

process_dependency_level = 2;

